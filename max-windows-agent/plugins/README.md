# MAX Plugins Directory

This directory is reserved for modular tools and plugins that extend the capability of the MAX Agent.

## Future Directories
- `plugins/adb/`: Client connectors for Android Debug Bridge.
- `plugins/vision/`: Image classification and OCR handlers.
- `plugins/clipboard/`: Interprocess clipboard synchronization.
- `plugins/filesystem/`: Remote filesystem inspection utilities.
